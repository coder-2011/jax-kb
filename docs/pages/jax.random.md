- [](index.html)
- [API Reference](jax.html)
- `jax.random` module

[ ](https://github.com/jax-ml/jax "Source repository")

- [ .rst](_sources/jax.random.rst "Download source file")
-  .pdf

# jax.random module

## Contents

- [Basic usage](#basic-usage)
- [PRNG keys](#prng-keys)
- [Advanced](#advanced)
  - [Design and background](#design-and-background)
  - [Advanced RNG configuration](#advanced-rng-configuration)
- [API Reference](#api-reference)
  - [Key Creation & Manipulation](#key-creation-manipulation)
  - [Random Samplers](#random-samplers)

# `jax.random` module[\#](#module-jax.random "Link to this heading")

Utilities for pseudo-random number generation.

The [`jax.random`](#module-jax.random "jax.random") package provides a number of routines for deterministic generation of sequences of pseudorandom numbers.

## Basic usage[\#](#basic-usage "Link to this heading")

    >>> seed = 1701
    >>> num_steps = 100
    >>> key = jax.random.key(seed)
    >>> for i in range(num_steps):
    ...   key, subkey = jax.random.split(key)
    ...   params = compiled_update(subkey, params, next(batches))  

## PRNG keys[\#](#prng-keys "Link to this heading")

Unlike the *stateful* pseudorandom number generators (PRNGs) that users of NumPy and SciPy may be accustomed to, JAX random functions all require an explicit PRNG state to be passed as a first argument. The random state is described by a special array element type that we call a **key**, usually generated by the [`jax.random.key()`](_autosummary/jax.random.key.html#jax.random.key "jax.random.key") function:

    >>> from jax import random
    >>> key = random.key(0)
    >>> key
    Array((), dtype=key<fry>) overlaying:
    [0 0]

This key can then be used in any of JAX’s random number generation routines:

    >>> random.uniform(key)
    Array(0.947667, dtype=float32)

Note that using a key does not modify it, so reusing the same key will lead to the same result:

    >>> random.uniform(key)
    Array(0.947667, dtype=float32)

If you need a new random number, you can use [`jax.random.split()`](_autosummary/jax.random.split.html#jax.random.split "jax.random.split") to generate new subkeys:

    >>> key, subkey = random.split(key)
    >>> random.uniform(subkey)
    Array(0.00729382, dtype=float32)

Note

Typed key arrays, with element types such as `key<fry>` above, were introduced in JAX v0.4.16. Before then, keys were conventionally represented in `uint32` arrays, whose final dimension represented the key’s bit-level representation.

Both forms of key array can still be created and used with the [`jax.random`](#module-jax.random "jax.random") module. New-style typed key arrays are made with [`jax.random.key()`](_autosummary/jax.random.key.html#jax.random.key "jax.random.key"). Legacy `uint32` key arrays are made with [`jax.random.PRNGKey()`](_autosummary/jax.random.PRNGKey.html#jax.random.PRNGKey "jax.random.PRNGKey").

To convert between the two, use [`jax.random.key_data()`](_autosummary/jax.random.key_data.html#jax.random.key_data "jax.random.key_data") and [`jax.random.wrap_key_data()`](_autosummary/jax.random.wrap_key_data.html#jax.random.wrap_key_data "jax.random.wrap_key_data"). The legacy key format may be needed when interfacing with systems outside of JAX (e.g. exporting arrays to a serializable format), or when passing keys to JAX-based libraries that assume the legacy format.

Otherwise, typed keys are recommended. Caveats of legacy keys relative to typed ones include:

- They have an extra trailing dimension.

- They have a numeric dtype (`uint32`), allowing for operations that are typically not meant to be carried out over keys, such as integer arithmetic.

- They do not carry information about the RNG implementation. When legacy keys are passed to [`jax.random`](#module-jax.random "jax.random") functions, a global configuration setting determines the RNG implementation (see “Advanced RNG configuration” below).

To learn more about this upgrade, and the design of key types, see [JEP 9263](https://docs.jax.dev/en/latest/jep/9263-typed-keys.html).

## Advanced[\#](#advanced "Link to this heading")

### Design and background[\#](#design-and-background "Link to this heading")

**TLDR**: JAX PRNG = [Threefry counter PRNG](http://www.thesalmons.org/john/random123/papers/random123sc11.pdf) + a functional array-oriented [splitting model](https://dl.acm.org/citation.cfm?id=2503784)

See [docs/jep/263-prng.md](https://github.com/jax-ml/jax/blob/main/docs/jep/263-prng.md) for more details.

To summarize, among other requirements, the JAX PRNG aims to:

1.  ensure reproducibility,

2.  parallelize well, both in terms of vectorization (generating array values) and multi-replica, multi-core computation. In particular it should not use sequencing constraints between random function calls.

### Advanced RNG configuration[\#](#advanced-rng-configuration "Link to this heading")

JAX provides several PRNG implementations. A specific one can be selected with the optional `impl` keyword argument to `jax.random.key`. When no `impl` option is passed to the `key` constructor, the implementation is determined by the global `jax_default_prng_impl` configuration flag. The string names of available implementations are:

- `"threefry2x32"` (**default**) and `"threefry4x32"`: Counter-based PRNGs based on a variant of the Threefry hash function, as described in [Salmon et al (2011)](https://dl.acm.org/doi/10.1145/2063384.2063405). `threefry2x32` has a 64-bit key space and a 64-bit counter space, while `threefry4x32` has a 128-bit key space and a 128-bit counter space.

- `"philox2x32"` and `"philox4x32"`: Counter-based PRNGs based on a variant of the Philox hash function, also described in [Salmon et al (2011)](https://dl.acm.org/doi/10.1145/2063384.2063405). `philox2x32` has a 32-bit key space and a 64-bit counter space, while `philox4x32` has a 64-bit key space and a 128-bit counter space.

- `"rbg"` and `"unsafe_rbg"` (**experimental**): PRNGs built atop [XLA’s Random Bit Generator (RBG) algorithm](https://openxla.org/xla/operation_semantics#rngbitgenerator).

  - `"rbg"` uses XLA RBG for random number generation, whereas for key derivation (as in `jax.random.split` and `jax.random.fold_in`) it uses the same method as `"threefry2x32"`.

  - `"unsafe_rbg"` uses XLA RBG for both generation as well as key derivation.

  Random numbers generated by these experimental schemes have not been subject to empirical randomness testing (e.g. BigCrush).

  Key derivation in `"unsafe_rbg"` has also not been empirically tested. The name emphasizes “unsafe” because key derivation quality and generation quality are not well understood.

  Additionally, both `"rbg"` and `"unsafe_rbg"` behave unusually under `jax.vmap`. When vmapping a random function over a batch of keys, its output values can differ from its true map over the same keys. Instead, under `vmap`, the entire batch of output random numbers is generated from only the first key in the input key batch. For example, if `keys` is a vector of 8 keys, then `jax.vmap(jax.random.normal)(keys)` equals `jax.random.normal(keys[0],`` ``shape=(8,))`. This peculiarity reflects a workaround to XLA RBG’s limited batching support.

Reasons to use an alternative to the default RNG include that:

1.  It may be slow to compile for TPUs.

2.  It is relatively slower to execute on TPUs.

**Automatic partitioning:**

In order for `jax.jit` to efficiently auto-partition functions that generate sharded random number arrays (or key arrays), all PRNG implementations depend on extra flags:

- For `"threefry2x32"`, and `"rbg"` key derivation, have `jax_threefry_partitionable=True`. As of JAX v.0.5.0, this is the default.

- For `"unsafe_rbg"`, and `"rbg"` random generation”, set the XLA flag `--xla_tpu_spmd_rng_bit_generator_unsafe=1`.

The XLA flag can be set using an the `XLA_FLAGS` environment variable, e.g. as `XLA_FLAGS=--xla_tpu_spmd_rng_bit_generator_unsafe=1`.

For more about `jax_threefry_partitionable`, see [jax-ml/jax#18480](https://github.com/jax-ml/jax/discussions/18480)

**Summary:**

| Property | Threefry | Threefry\* | Philox | rbg | unsafe_rbg | rbg\*\* | unsafe_rbg\*\* |
|----|----|----|----|----|----|----|----|
| Fastest on TPU |  |  |  | ✅ | ✅ | ✅ | ✅ |
| efficiently shardable (w/ pjit) |  | ✅ | ✅ |  |  | ✅ | ✅ |
| identical across shardings | ✅ | ✅ | ✅ | ✅ | ✅ |  |  |
| identical across CPU/GPU/TPU | ✅ | ✅ | ✅ |  |  |  |  |
| exact `jax.vmap` over keys | ✅ | ✅ | ✅ |  |  |  |  |

(\*): with `jax_threefry_partitionable=1` set (default as of JAX v0.5.0)

(\*\*): with `XLA_FLAGS=--xla_tpu_spmd_rng_bit_generator_unsafe=1` set

## API Reference[\#](#api-reference "Link to this heading")

### Key Creation & Manipulation[\#](#key-creation-manipulation "Link to this heading")

|  |  |
|----|----|
| [`key`](_autosummary/jax.random.key.html#jax.random.key "jax.random.key")(seed, \*\[, impl, dtype\]) | Create a pseudo-random number generator (PRNG) key given an integer seed. |
| [`key_dtype`](_autosummary/jax.random.key_dtype.html#jax.random.key_dtype "jax.random.key_dtype")(\[impl_spec\]) | Get the dtype corresponding to a PRNG implementation. |
| [`key_data`](_autosummary/jax.random.key_data.html#jax.random.key_data "jax.random.key_data")(keys) | Recover the bits of key data underlying a PRNG key array. |
| [`wrap_key_data`](_autosummary/jax.random.wrap_key_data.html#jax.random.wrap_key_data "jax.random.wrap_key_data")(key_bits_array, \*\[, impl, dtype\]) | Wrap an array of key data bits into a PRNG key array. |
| [`fold_in`](_autosummary/jax.random.fold_in.html#jax.random.fold_in "jax.random.fold_in")(key, data) | Folds in data to a PRNG key to form a new PRNG key. |
| [`split`](_autosummary/jax.random.split.html#jax.random.split "jax.random.split")(key\[, num\]) | Splits a PRNG key into num new keys by adding a leading axis. |
| [`clone`](_autosummary/jax.random.clone.html#jax.random.clone "jax.random.clone")(key) | Clone a key for reuse |
| [`PRNGKey`](_autosummary/jax.random.PRNGKey.html#jax.random.PRNGKey "jax.random.PRNGKey")(seed, \*\[, impl\]) | Create a legacy PRNG key given an integer seed. |

### Random Samplers[\#](#random-samplers "Link to this heading")

|  |  |
|----|----|
| [`ball`](_autosummary/jax.random.ball.html#jax.random.ball "jax.random.ball")(key, d\[, p, shape, dtype, out_sharding\]) | Sample uniformly from the unit Lp ball. |
| [`bernoulli`](_autosummary/jax.random.bernoulli.html#jax.random.bernoulli "jax.random.bernoulli")(key\[, p, shape, mode, out_sharding\]) | Sample Bernoulli random values with given shape and mean. |
| [`beta`](_autosummary/jax.random.beta.html#jax.random.beta "jax.random.beta")(key, a, b\[, shape, dtype, out_sharding\]) | Sample Beta random values with given shape and float dtype. |
| [`binomial`](_autosummary/jax.random.binomial.html#jax.random.binomial "jax.random.binomial")(key, n, p\[, shape, dtype\]) | Sample Binomial random values with given shape and float dtype. |
| [`bits`](_autosummary/jax.random.bits.html#jax.random.bits "jax.random.bits")(key\[, shape, dtype, out_sharding\]) | Sample uniform bits in the form of unsigned integers. |
| [`categorical`](_autosummary/jax.random.categorical.html#jax.random.categorical "jax.random.categorical")(key, logits\[, axis, shape, ...\]) | Sample random values from categorical distributions. |
| [`cauchy`](_autosummary/jax.random.cauchy.html#jax.random.cauchy "jax.random.cauchy")(key\[, shape, dtype, out_sharding\]) | Sample Cauchy random values with given shape and float dtype. |
| [`chisquare`](_autosummary/jax.random.chisquare.html#jax.random.chisquare "jax.random.chisquare")(key, df\[, shape, dtype, out_sharding\]) | Sample Chisquare random values with given shape and float dtype. |
| [`choice`](_autosummary/jax.random.choice.html#jax.random.choice "jax.random.choice")(key, a\[, shape, replace, p, axis, mode\]) | Generates a random sample from a given array. |
| [`dirichlet`](_autosummary/jax.random.dirichlet.html#jax.random.dirichlet "jax.random.dirichlet")(key, alpha\[, shape, dtype, ...\]) | Sample Dirichlet random values with given shape and float dtype. |
| [`double_sided_maxwell`](_autosummary/jax.random.double_sided_maxwell.html#jax.random.double_sided_maxwell "jax.random.double_sided_maxwell")(key, loc, scale\[, ...\]) | Sample from a double sided Maxwell distribution. |
| [`exponential`](_autosummary/jax.random.exponential.html#jax.random.exponential "jax.random.exponential")(key\[, shape, dtype, out_sharding\]) | Sample Exponential random values with given shape and float dtype. |
| [`f`](_autosummary/jax.random.f.html#jax.random.f "jax.random.f")(key, dfnum, dfden\[, shape, dtype, ...\]) | Sample F-distribution random values with given shape and float dtype. |
| [`gamma`](_autosummary/jax.random.gamma.html#jax.random.gamma "jax.random.gamma")(key, a\[, shape, dtype, out_sharding\]) | Sample Gamma random values with given shape and float dtype. |
| [`generalized_normal`](_autosummary/jax.random.generalized_normal.html#jax.random.generalized_normal "jax.random.generalized_normal")(key, p\[, shape, dtype, ...\]) | Sample from the generalized normal distribution. |
| [`geometric`](_autosummary/jax.random.geometric.html#jax.random.geometric "jax.random.geometric")(key, p\[, shape, dtype, out_sharding\]) | Sample Geometric random values with given shape and float dtype. |
| [`gumbel`](_autosummary/jax.random.gumbel.html#jax.random.gumbel "jax.random.gumbel")(key\[, shape, dtype, mode, out_sharding\]) | Sample Gumbel random values with given shape and float dtype. |
| [`laplace`](_autosummary/jax.random.laplace.html#jax.random.laplace "jax.random.laplace")(key\[, shape, dtype, out_sharding\]) | Sample Laplace random values with given shape and float dtype. |
| [`loggamma`](_autosummary/jax.random.loggamma.html#jax.random.loggamma "jax.random.loggamma")(key, a\[, shape, dtype, out_sharding\]) | Sample log-gamma random values with given shape and float dtype. |
| [`logistic`](_autosummary/jax.random.logistic.html#jax.random.logistic "jax.random.logistic")(key\[, shape, dtype, out_sharding\]) | Sample logistic random values with given shape and float dtype. |
| [`lognormal`](_autosummary/jax.random.lognormal.html#jax.random.lognormal "jax.random.lognormal")(key\[, sigma, shape, dtype, ...\]) | Sample lognormal random values with given shape and float dtype. |
| [`maxwell`](_autosummary/jax.random.maxwell.html#jax.random.maxwell "jax.random.maxwell")(key\[, shape, dtype, out_sharding\]) | Sample from a one sided Maxwell distribution. |
| [`multinomial`](_autosummary/jax.random.multinomial.html#jax.random.multinomial "jax.random.multinomial")(key, n, p, \*\[, shape, dtype, unroll\]) | Sample from a multinomial distribution. |
| [`multivariate_normal`](_autosummary/jax.random.multivariate_normal.html#jax.random.multivariate_normal "jax.random.multivariate_normal")(key, mean, cov\[, shape, ...\]) | Sample multivariate normal random values with given mean and covariance. |
| [`normal`](_autosummary/jax.random.normal.html#jax.random.normal "jax.random.normal")(key\[, shape, dtype, out_sharding\]) | Sample standard normal random values with given shape and float dtype. |
| [`orthogonal`](_autosummary/jax.random.orthogonal.html#jax.random.orthogonal "jax.random.orthogonal")(key, n\[, shape, dtype, m, ...\]) | Sample uniformly from the orthogonal group O(n). |
| [`pareto`](_autosummary/jax.random.pareto.html#jax.random.pareto "jax.random.pareto")(key, b\[, shape, dtype, out_sharding\]) | Sample Pareto random values with given shape and float dtype. |
| [`permutation`](_autosummary/jax.random.permutation.html#jax.random.permutation "jax.random.permutation")(key, x\[, axis, independent, ...\]) | Returns a randomly permuted array or range. |
| [`poisson`](_autosummary/jax.random.poisson.html#jax.random.poisson "jax.random.poisson")(key, lam\[, shape, dtype, out_sharding\]) | Sample Poisson random values with given shape and integer dtype. |
| [`rademacher`](_autosummary/jax.random.rademacher.html#jax.random.rademacher "jax.random.rademacher")(key\[, shape, dtype, out_sharding\]) | Sample from a Rademacher distribution. |
| [`randint`](_autosummary/jax.random.randint.html#jax.random.randint "jax.random.randint")(key, shape, minval, maxval\[, dtype, ...\]) | Sample uniform random values in \[minval, maxval) with given shape/dtype. |
| [`rayleigh`](_autosummary/jax.random.rayleigh.html#jax.random.rayleigh "jax.random.rayleigh")(key, scale\[, shape, dtype, ...\]) | Sample Rayleigh random values with given shape and float dtype. |
| [`t`](_autosummary/jax.random.t.html#jax.random.t "jax.random.t")(key, df\[, shape, dtype, out_sharding\]) | Sample Student's t random values with given shape and float dtype. |
| [`triangular`](_autosummary/jax.random.triangular.html#jax.random.triangular "jax.random.triangular")(key, left, mode, right\[, shape, ...\]) | Sample Triangular random values with given shape and float dtype. |
| [`truncated_normal`](_autosummary/jax.random.truncated_normal.html#jax.random.truncated_normal "jax.random.truncated_normal")(key, lower, upper\[, shape, ...\]) | Sample truncated standard normal random values with given shape and dtype. |
| [`uniform`](_autosummary/jax.random.uniform.html#jax.random.uniform "jax.random.uniform")(key\[, shape, dtype, minval, maxval, ...\]) | Sample uniform random values in \[minval, maxval) with given shape/dtype. |
| [`wald`](_autosummary/jax.random.wald.html#jax.random.wald "jax.random.wald")(key, mean\[, shape, dtype, out_sharding\]) | Sample Wald random values with given shape and float dtype. |
| [`weibull_min`](_autosummary/jax.random.weibull_min.html#jax.random.weibull_min "jax.random.weibull_min")(key, scale, concentration\[, ...\]) | Sample from a Weibull distribution. |

[](_autosummary/jax.lax.linalg.tridiagonal_solve.html "previous page")

previous

jax.lax.linalg.tridiagonal_solve

[](_autosummary/jax.random.key.html "next page")

next

jax.random.key

Contents

- [Basic usage](#basic-usage)
- [PRNG keys](#prng-keys)
- [Advanced](#advanced)
  - [Design and background](#design-and-background)
  - [Advanced RNG configuration](#advanced-rng-configuration)
- [API Reference](#api-reference)
  - [Key Creation & Manipulation](#key-creation-manipulation)
  - [Random Samplers](#random-samplers)

By The JAX authors

© Copyright 2024, The JAX Authors.\
